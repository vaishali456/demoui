import { ChangeDetectorRef, TemplateRef, OnChanges, AfterContentInit } from '@angular/core';
import { NglFormInput } from './input';
import { NglFormLabelTemplate } from '../form-label';
export declare class NglFormElement implements OnChanges, AfterContentInit {
    protected detector: ChangeDetectorRef;
    contentEl: NglFormInput;
    labelStr: string;
    labelTpl: NglFormLabelTemplate;
    error: string;
    uid: string;
    required: boolean;
    _label: TemplateRef<any> | string;
    constructor(detector: ChangeDetectorRef);
    ngOnChanges(changes?: any): void;
    ngAfterContentInit(): void;
    setRequired(required: string | boolean): void;
    protected setInputErrorId(): void;
    protected setFormLabel(): void;
}
