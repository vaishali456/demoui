import { ChangeDetectorRef, AfterContentInit } from '@angular/core';
import { NglFormElement } from './element';
import { NglFormCheckbox } from './input';
import { NglFormLabelTemplate } from '../form-label';
export declare class NglFormElementCheckboxToggle extends NglFormElement implements AfterContentInit {
    contentEl: NglFormCheckbox;
    labelStr: string;
    labelTpl: NglFormLabelTemplate;
    error: string;
    enabledText: string;
    disabledText: string;
    constructor(detector: ChangeDetectorRef);
    ngAfterContentInit(): void;
}
