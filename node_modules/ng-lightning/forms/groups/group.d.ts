import { TemplateRef, OnChanges } from '@angular/core';
import { NglFormLabelTemplate } from '../form-label';
export declare class NglFormGroup implements OnChanges {
    labelStr: string;
    labelTpl: NglFormLabelTemplate;
    error: string;
    required: boolean;
    uid: string;
    _label: string | TemplateRef<any>;
    ngOnChanges(changes?: any): void;
    ngAfterContentInit(): void;
    private setFormLabel();
}
