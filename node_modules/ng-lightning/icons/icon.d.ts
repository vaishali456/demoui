import { ElementRef, Renderer2, OnChanges } from '@angular/core';
import { NglButton } from '../buttons/button';
import { NglButtonIcon } from '../buttons/button-icon';
export declare type NglIconCategory = 'action' | 'custom' | 'doctype' | 'standard' | 'utility';
export declare class NglIcon implements OnChanges {
    element: ElementRef;
    renderer: Renderer2;
    private state;
    _icon: string;
    setIcon: string;
    readonly icon: string;
    setCategory: NglIconCategory;
    type: 'default' | 'warning' | 'error';
    align: 'left' | 'right';
    size: 'x-small' | 'small' | 'large';
    alt: string;
    svgClass: string | string[];
    category: NglIconCategory;
    private button;
    private _containerClass;
    constructor(element: ElementRef, renderer: Renderer2, state: string, button: string, nglButton: NglButton, nglButtonIcon: NglButtonIcon);
    ngOnChanges(changes?: any): void;
    svgClasses(): string[];
    private readonly containerClass;
}
