import { ElementRef, TemplateRef, ViewContainerRef, Renderer2, ComponentFactoryResolver, Injector, EventEmitter, NgZone } from '@angular/core';
import 'rxjs/add/operator/take';
import { Direction } from './popover';
export declare class NglPopoverTrigger {
    private element;
    private viewContainer;
    private injector;
    private ngZone;
    private renderer;
    nglPopover: string | TemplateRef<any>;
    nglPopoverHeader: string;
    nglPopoverFooter: string;
    nglPopoverPlacement: Direction;
    nglPopoverTheme: string;
    nglTooltip: string | boolean;
    nglPopoverDelay: any | any[];
    nglInteractive: boolean | string;
    nglOpen: boolean;
    nglPopoverToggled: EventEmitter<boolean>;
    private popover;
    private popoverFactory;
    private componentRef;
    private viewRef;
    private placement;
    private theme;
    private tether;
    private openDelay;
    private closeDelay;
    private toggleTimeout;
    private interactive;
    private interactiveSubscription;
    constructor(element: ElementRef, viewContainer: ViewContainerRef, injector: Injector, ngZone: NgZone, renderer: Renderer2, componentFactoryResolver: ComponentFactoryResolver);
    open(delay?: number): void;
    close(delay?: number): void;
    position(async?: boolean): void;
    ngOnDestroy(): void;
    private toggle(open, delay);
    private setTether(create?);
    private setPopover();
    private create();
    private getProjectableNodes();
    private destroy();
}
