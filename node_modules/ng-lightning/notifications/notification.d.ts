import { EventEmitter, OnChanges, ElementRef, Renderer2 } from '@angular/core';
import { NglNotificationClose } from './notification-close';
export declare class NglNotification implements OnChanges {
    element: ElementRef;
    renderer: Renderer2;
    type: 'toast' | 'alert';
    setSeverity: string;
    assistiveText: string;
    closeAssistiveText: string;
    timeout: number;
    closeEventEmitter: EventEmitter<string>;
    severity: string;
    showClose: boolean;
    private currentTimeout;
    constructor(element: ElementRef, renderer: Renderer2, notificationClose: NglNotificationClose);
    ngOnChanges(changes: any): void;
    close(reason?: string, $event?: Event): void;
    ngOnDestroy(): void;
    private clearTimeout();
}
